[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "https://www.owlbear.rodeo"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

[[headers]]
  for = "/manifest.json"
  [headers.values]
    Access-Control-Allow-Origin = "https://www.owlbear.rodeo"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

[[headers]]
  for = "/index.html"
  [headers.values]
    Access-Control-Allow-Origin = "https://www.owlbear.rodeo"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

[[headers]]
  for = "/js/*"
  [headers.values]
    Access-Control-Allow-Origin = "https://www.owlbear.rodeo"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

[[headers]]
  for = "/css/*"
  [headers.values]
    Access-Control-Allow-Origin = "https://www.owlbear.rodeo"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

[[headers]]
  for = "/icon.svg"
  [headers.values]
    Access-Control-Allow-Origin = "https://www.owlbear.rodeo"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

# Configuración de build
[build]
  command = "node scripts/build-config.js"
  publish = "."
  functions = "netlify/functions"

# Variables de entorno (configúralas en Netlify Dashboard)
# NOTION_API_TOKEN debe estar configurada en: Site settings → Environment variables

